
spring:
  datasource:
    password:
    url: jdbc:mysql://localhost:3306/serverdb
    username: root
  jpa:
    hibernate:
      dll-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MYSQL8Dialect
        format_sql: true
    show-sql: true
